apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: herik-frontend
spec:
  # modify replicas according to your case
  replicas: 5
  selector:
    matchLabels:
      apptype: frontend
      env: prod
      